#!/bin/sh


# run this command on client machine to prepare for ssh connection with raspberrypi
sudo systemctl start ssh


# run this command on the raspberry pi that servers as a ssh server
sudo systemctl start ssh


# I'm still working on this script, haha
